import generateClasses from '@/utils/generateClasses';

export const zoomClasses = generateClasses('Zoom', [
  'loader',
  'root',
  'toolbar',
  'toolbarTitle',
  'table',
  'lastUpdate',
]);

export default zoomClasses;
